#House Robber

class Solution:
    def rob(self, nums: List[int]) -> int:
        if not nums:
            return 0
        n = len(nums)
        if n == 1:
            return nums[0]
        p1 , p2 = nums[0],0
        for i in range(1,n):
            curr = max(p1,p2+nums[i])
            p2,p1=p1,curr
        return p1

#Subsets

from typing import List

class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        res = []
        currentres = []

        def subs(i, nums, currentres, res):
            if i == len(nums):
                res.append(currentres[:])  
                return 
            
            
            currentres.append(nums[i])
            subs(i + 1, nums, currentres, res)
            
            
            currentres.pop()
            subs(i + 1, nums, currentres, res)


        subs(0, nums, currentres, res)
        return res

 #Jump Game

class Solution:
    def canJump(self, a: List[int]) -> bool:
        if len(a) == 1:
            return True

        reachable = [False] * len(a)
        reachable[0] = True   

        for i in range(len(a)):
            if not reachable[i]:   
                continue
            for j in range(1, a[i] + 1):
                if i + j >= len(a) - 1:
                    return True
                reachable[i + j] = True

        return False

#First Missing Positive

class Solution:
    def ladderLength(self, beginWord: str, endWord: str, wordList: List[str]) -> int:
        q  = deque()
        wordset = set(wordList)
        q.append([beginWord])
        wordset.discard(beginWord)
        mintrans = 1

        while q:
            currlist = q.popleft()
            a = []
            for ele in currlist:
                if ele == endWord:
                    return mintrans
                l = list(ele)
                for i in range(len(l)):
                    ch = l[i]
                    for c in range(97,123):
                        l[i] = chr(c)
                        s = "".join(l)
                        if s in wordset:
                            a.append(s)
                            wordset.remove(s)
                    l[i]= ch
            if a!=[]:
                print(a)
                q.append(a)
                mintrans+=1
        return 0
